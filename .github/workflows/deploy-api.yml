name: Deploy API

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: api-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      ACR_REGISTRY: ${{ secrets.ACR_REGISTRY }}
      ACR_NAMESPACE: ${{ secrets.ACR_NAMESPACE }}
      ACR_REPOSITORY: ai-clash-api
      REMOTE_DIR: ${{ secrets.API_REMOTE_DIR || '/opt/ai-clash-api' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: cd packages/api && bun install --frozen-lockfile

      - name: Build API
        run: cd packages/api && bun build

      - name: Log in to ACR
        run: echo "${{ secrets.ACR_PASSWORD }}" | docker login "$ACR_REGISTRY" -u "${{ secrets.ACR_USERNAME }}" --password-stdin

      - name: Build image
        run: |
          IMAGE="$ACR_REGISTRY/$ACR_NAMESPACE/$ACR_REPOSITORY"
          docker build -f packages/api/Dockerfile -t "$IMAGE:${{ github.sha }}" -t "$IMAGE:latest" .

      - name: Push image
        run: |
          IMAGE="$ACR_REGISTRY/$ACR_NAMESPACE/$ACR_REPOSITORY"
          docker push "$IMAGE:${{ github.sha }}"
          docker push "$IMAGE:latest"

      # - name: Deploy on server
      #   env:
      #     SERVER_HOST: ${{ secrets.SERVER_HOST }}
      #     SERVER_USER: ${{ secrets.SERVER_USER }}
      #     SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
      #     ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
      #     ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
      #   run: |
      #     mkdir -p ~/.ssh
      #     printf '%s\n' "$SERVER_SSH_KEY" > ~/.ssh/id_rsa
      #     chmod 600 ~/.ssh/id_rsa

      #     ACR_USERNAME_B64="$(printf '%s' "$ACR_USERNAME" | base64 -w0)"
      #     ACR_PASSWORD_B64="$(printf '%s' "$ACR_PASSWORD" | base64 -w0)"

      #     ssh -o StrictHostKeyChecking=no "$SERVER_USER@$SERVER_HOST" \
      #       "ACR_USERNAME=\$(printf '%s' '$ACR_USERNAME_B64' | base64 -d) && \
      #        ACR_PASSWORD=\$(printf '%s' '$ACR_PASSWORD_B64' | base64 -d) && \
      #        echo \"\$ACR_PASSWORD\" | docker login '$ACR_REGISTRY' -u \"\$ACR_USERNAME\" --password-stdin && \
      #        cd '$REMOTE_DIR' && \
      #        docker compose pull ai-clash-api && \
      #        docker compose run --rm ai-clash-api node dist/migrate.js && \
      #        docker compose up -d ai-clash-api && \
      #        curl -fsS http://127.0.0.1:8080/healthz"
